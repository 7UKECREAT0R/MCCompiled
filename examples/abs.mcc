// A function for determining absolute value.
// Note that the return values aren't actually printed, this is just an example.

function abs(int number) {
    if number < 0
        return number * -1
    else
        return number
}

abs(-50)  // 50
abs(50)   // 50
abs(-3)   // 3
abs(0)    // 0
abs(12)   // 12
