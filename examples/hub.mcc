// This example creates a hub system which can be set and teleported to from anywhere.

// Enable the nulls feature.
feature nulls

// Sets where the hub is.
function sethub {
	null single "hub" ~ ~ ~
	print "The hub has been set here!"
}

// Teleports to the hub.
function hub {
	tp "hub"
	print "Teleported to hub!"
}
